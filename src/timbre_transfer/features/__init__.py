"""Optional DDSP feature extraction (pitch + loudness).

This subpackage is EXPERIMENTAL and requires the optional ``ddsp`` extra
(``pip install -e .[ddsp]`` or uncomment torchcrepe/librosa in requirements.txt).
"""
